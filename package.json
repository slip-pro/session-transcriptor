{
  "name": "session-transcriptor",
  "version": "1.0.0",
  "description": "CLI tool for coaching session transcription via Claude Code skill",
  "type": "module",
  "scripts": {
    "transcribe": "tsx src/cli/transcribe.ts",
    "generate": "tsx src/cli/generate-docx.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@deepgram/sdk": "^3.9.0",
    "docx": "^9.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
